# Copy this file to ".env" and paste your key.
# This value stays server-side (read by the Vite dev middleware in
# server/extractPlugin.js) and is NEVER exposed to the browser. Do not add a
# VITE_ prefix — that would leak it into client code.
ANTHROPIC_API_KEY=sk-ant-...
